sudo: false

language: node_js

node_js:
  - '4.1.0'
  - '4.0.0'
  - '0.10'
  - '0.12'
  - '4.2'
  - '5.1'

# in my repo, test all branches
# branches:
#  only:
#    - master
#    - travis-ci

# Not using `npm install --dev` because it is recursive. It will pull in the all
# development dependencies for CoffeeScript. Way too much spew in the Travis CI
# build output.

before_install:
  - npm install
  - make zic && make
